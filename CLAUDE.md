## Agent skills

### Issue tracker

Issues live as GitHub issues in `oreoolis/meong-MyWay`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
